package com.example.implwebsitev2.repository;

import com.example.implwebsitev2.domain.WriteReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WriteReportRepository extends JpaRepository<WriteReport, Long> {
}
